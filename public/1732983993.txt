#Try fixing this code
 FUN scene()
    VAR A = "Nila"
    PRINT(A)
    FOR i = 3 TO 4 THEN
       PRINT(A)
    END
    action(A)
END

FUN action(actor)
    VAR len = 0
    IF len == 4 THEN 
        PRINT("Odi Vaa....")
    ELSE
        PRINT("Odi poidu...")
    END
END

scene()